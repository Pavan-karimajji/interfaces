syntax = "proto3";
package adas.lane;
import "common/common.proto";

// PROVENANCE: original architecture-session design, predates this project's
// reference-mining exercise. NOT verified against Continental reference material —
// do not assume these field names/shapes are sourced. See
// docs/interfaces_types_migration.md §0 for the verified-vs-legacy boundary.
message LaneBoundary {
  uint32 boundary_id = 1;
  LaneMarkingType marking_type = 2;
  repeated adas.common.Point2D points = 3;
  float confidence = 4;
  float c0 = 5;
  float c1 = 6;
  float c2 = 7;
  float c3 = 8;
}

message LaneModel {
  adas.common.Header header = 1;
  LaneBoundary left_boundary = 2;
  LaneBoundary right_boundary = 3;
  float lane_width_m = 4;
  float curvature = 5;
  bool lane_valid = 6;
}

enum LaneMarkingType {
  MARKING_UNKNOWN = 0;
  MARKING_SOLID = 1;
  MARKING_DASHED = 2;
  MARKING_DOUBLE_SOLID = 3;
  MARKING_ROAD_EDGE = 4;
  MARKING_NONE = 5;
}
